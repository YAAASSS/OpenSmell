# RFC-0007: Stimulus and Observation Resource Model

- Status: Draft
- Category: Experimental
- Target: Future OpenSmell version
- Last updated: 2026-09-04
- Depends on: RFC-0003, RFC-0005, RFC-0006
- Does not modify: OpenSmell 0.1 Core

## 1. Abstract

This RFC proposes an experimental resource model for representing olfactory

stimuli, observation targets, observations, and scheme-defined observation

results in OpenSmell.

The model introduces the following concepts:

- `Reference`

- `ExternalIdentifier`

- `Condition`

- `Stimulus`

- `ObservationTarget`

- `Observation`

- `Result`

The central architectural distinction is:

```text

Odor or source resource

        |

        v

     Stimulus

        |

        v

   Observation -----> ObservationTarget

        |

        v

     Result(s)

```

An odor, molecule, or mixture describes something that may participate in an

olfactory stimulus.

A stimulus describes something presented or applied under particular

conditions.

An observation describes a result associated with a stimulus and, optionally,

an observation target.

A result is interpreted according to a scheme.

This RFC deliberately does not define a universal measurement model.

Instead, observations contain one or more scheme-defined `Result` resources

whose internal data are interpreted according to the declared result scheme.

This architecture has been experimentally evaluated against two substantially

different real-world olfactory datasets:

1\. Burton et al. 2022 physiological mouse olfactory bulb measurements.

2\. Keller/Vosshall human psychophysical odor measurements.

Together, these experiments represented 256,658 observations without requiring

a universal observation measurement structure.

This RFC remains experimental.

Nothing defined here is part of the OpenSmell 0.1 Core specification.

---

## 2. Status and Scope

### 2.1 Status

This document is a Draft RFC.

The structures described here are experimental.

Implementations MUST NOT assume that the structures or identifiers described

in this RFC are stable until the RFC is promoted to a normative status.

In particular:

- the Python experimental API may change;

- field names may change;

- result schemes may change;

- serialization rules are not yet normative;

- resource graph storage is not yet normative;

- the experimental namespace used by current prototypes is not automatically

  reserved for future normative use.

---

### 2.2 Non-goals

RFC-0007 does NOT attempt to define:

- a universal biological measurement scheme;

- a universal categorical observation scheme;

- a universal sensor output scheme;

- a universal model prediction scheme;

- a universal condition vocabulary;

- a universal unit system;

- a universal target taxonomy;

- a universal experimental protocol model;

- a universal provenance model;

- a normative graph serialization;

- a normative Result scheme registry;

- a normative UUID generation algorithm;

- a universal observation identity extraction algorithm;

- physical odor rendering;

- diffuser control;

- timing or scheduling.

---

## 3. Motivation and Core Model

### 3.1 Motivation

OpenSmell 0.1 focuses on describing an odor through one or more

representations.

That model is useful for statements such as:

```text

This odor has the semantic descriptor "coffee".

```

or:

```text

This molecule is represented by this SMILES string.

```

or:

```text

This odor has these perceptual properties.

```

However, experimental olfactory datasets frequently represent a different

kind of information.

They may describe:

- a molecule presented at a particular concentration;

- an odor presented at a particular dilution;

- a human rating after exposure;

- whether a participant detected an odor;

- a physiological response in an animal;

- the response of a receptor;

- the response of an olfactory bulb region;

- the output of an electronic sensor;

- the output of a computational model.

These are not all properties of the odor itself.

They are observations made under particular experimental circumstances.

Trying to store all such information directly inside `Odor` would collapse

several distinct concepts into one resource.

This RFC therefore introduces an explicit stimulus and observation layer.

---

### 3.2 Core distinction

The following resources MUST be conceptually distinguished:

```text

Odor / Molecule / Mixture

           |

           v

        Stimulus

           |

           v

       Observation

           |

           +------> ObservationTarget

           |

           v

        Result(s)

```

The following statements are therefore fundamental:

```text

Odor != Stimulus

Stimulus != Observation

Odor != Observation

ObservationTarget != Observation

Result != Observation

```

These distinctions are semantic, not merely implementation details.

---

### 3.3 Source resource

A `Stimulus` MAY reference another OpenSmell resource describing what was

presented.

Examples include:

- an `Odor`;

- a molecule;

- a mixture;

- a material;

- another future OpenSmell resource type.

This RFC does not define a universal source resource type.

The source is referenced using a `Reference`.

Example:

```json

{

  "source": {

    "resource\_id": "9c7b4d6c-..."

  }

}

```

The referenced resource might represent a molecule while the stimulus adds

experimental presentation conditions.

---

### 3.4 Odor is not Stimulus

An odor describes olfactory information.

A stimulus describes a presentation or application event or configuration.

For example:

```text

Molecule:

    vanillin

Stimulus A:

    vanillin

    dilution = 1/1000

Stimulus B:

    vanillin

    dilution = 1/100000

```

The same molecule participates in two different stimuli.

Therefore:

```text

1 molecule -> N stimuli

```

is valid and expected.

---

## 4. Terminology

### 4.1 Reference

A `Reference` identifies another OpenSmell resource.

Experimental conceptual form:

```json

{

  "resource\_id": "6136624f-5105-52f1-875e-53a177ef2134"

}

```

A reference contains:

```text

resource\_id

```

The referenced resource does not need to be embedded inside the referencing

resource.

---

### 4.2 ExternalIdentifier

An `ExternalIdentifier` preserves an identifier assigned by an external system

or source dataset.

Conceptual form:

```json

{

  "scheme": "pubchem.cid",

  "value": "240"

}

```

The fields are:

```text

scheme

value

```

Both are strings.

---

### 4.3 Stimulus

A `Stimulus` represents something presented to, or applied to, an observation

target under defined or partially defined conditions.

Experimental conceptual structure:

```json

{

  "id": "...",

  "source": {

    "resource\_id": "..."

  },

  "identifiers": [],

  "conditions": [],

  "extra": {}

}

```

---

### 4.4 Condition

A `Condition` describes one presentation condition associated with a

Stimulus.

Experimental conceptual structure:

```json

{

  "property": "concentration",

  "value": 0.000001,

  "unit": "mol/L"

}

```

or:

```json

{

  "property": "dilution",

  "value": "1/1000"

}

```

---

### 4.5 ObservationTarget

An `ObservationTarget` identifies the entity with respect to which an

Observation is made.

The term `target` is deliberately generic.

It MUST NOT be interpreted as meaning only a human participant.

---

### 4.6 Observation

An `Observation` represents a result obtained in association with a Stimulus

and optionally an ObservationTarget.

Experimental conceptual structure:

```json

{

  "id": "...",

  "stimulus": {

    "resource\_id": "..."

  },

  "target": {

    "resource\_id": "..."

  },

  "results": [],

  "context": {},

  "identifiers": [],

  "extra": {}

}

```

---

### 4.7 Result

A `Result` contains data interpreted according to a declared, versioned Result
scheme.

Experimental conceptual structure:

```json
{
  "scheme": {
    "id": "org.opensmell.perceptual.measurements",
    "version": "0.1"
  },
  "data": {
    "measurements": []
  },
  "extra": {}
}
```

In the current experimental Python model, `scheme` is represented by
`ResultScheme`, containing:

```text
id
version
extra
```

Both `id` and `version` MUST be non-empty strings.

`ResultScheme` is intentionally separate from the OpenSmell Core `Scheme` type
while RFC-0007 remains experimental.

## 5. References and External Identifiers

### 5.1 Reference semantics

A `Reference` expresses identity, not object containment.

For example:

```text

Observation

    stimulus -> Resource ID of Stimulus

```

does not imply that the complete Stimulus must be serialized inside the

Observation.

This enables graph-oriented representations.

---

### 5.2 Reference resolution

Structural validity and graph resolution SHOULD remain distinct concepts.

A reference MAY be structurally valid even when its target is not currently

available.

For example:

```json

{

  "resource\_id": "missing-resource"

}

```

may still be preserved by a parser or adapter.

An implementation MUST NOT automatically discard an unresolved reference.

---

### 5.3 Unresolved references

Unresolved references may occur because:

- a dataset is incomplete;

- resources are stored separately;

- resources are loaded incrementally;

- the source dataset contains inconsistent metadata;

- a remote resource is temporarily unavailable;

- a resource is intentionally external.

Therefore:

```text

unresolved reference != invalid reference

```

Graph validation MAY report unresolved references separately.

---

### 5.4 External identifiers are not Resource IDs

An external identifier MUST NOT automatically become an OpenSmell Resource ID.

For example:

```text

PubChem CID:

240

```

is an external identifier.

An OpenSmell Resource ID may instead be:

```text

c3120f5d-...

```

The two identifiers serve different purposes.

---

### 5.5 Source-native identifiers

Adapters SHOULD preserve identifiers genuinely supplied by their source

dataset.

Examples include:

```text

PubChem CID

Burton stimulus identifier

Burton ROI identifier

Keller/Vosshall subject identifier

```

Adapters SHOULD NOT invent external identifiers merely because a convenient

string can be constructed.

For example:

```text

CID\@dilution

```

MUST NOT be presented as a source-native external identifier unless the source

dataset actually defines that identifier.

---

### 5.6 Generated identity

OpenSmell Resource IDs MAY be generated from structured source identity as

described experimentally by RFC-0006.

For example, a stimulus source identity may be:

```json

{

  "cid": "240",

  "dilution": "1/1000"

}

```

The resulting OpenSmell Resource ID is generated identity.

The structured source identity does not need to be exposed as an external

identifier.

---

### 5.7 Identity and metadata

Resource identity and resource metadata are separate concerns.

A resource MAY have:

- an OpenSmell Resource ID;

- zero external identifiers;

- one external identifier;

- multiple external identifiers.

None of these imply semantic equality between resources.

---

## 6. Stimulus Model

### 6.1 Stimulus fields

The experimental `Stimulus` model contains:

```text

id

source

identifiers

conditions

extra

```

where:

- `id` identifies the Stimulus resource;

- `source` optionally references what was presented;

- `identifiers` preserves source-native external identifiers;

- `conditions` describes presentation conditions;

- `extra` preserves additional experimental information.

---

### 6.2 Stimulus source

`source` is optional.

A missing source means that the source is unknown, unavailable, intentionally

unspecified, or not represented.

It MUST NOT automatically imply:

- blank stimulus;

- control stimulus;

- odorless stimulus;

- no stimulus;

- invalid stimulus.

The meaning depends on the source dataset.

---

### 6.3 Source-less stimuli

The Burton dataset contains a stimulus whose source identifier is:

```text

empty

```

Its source molecule is not represented.

The experimental adapter preserves this as a valid `Stimulus` with:

```text

source = None

```

No semantic role such as `control` is invented.

This illustrates an important rule:

\> OpenSmell adapters SHOULD preserve source information without inventing

\> scientific meaning not supported by the source.

---

### 6.4 Condition fields

A Condition contains:

```text

property

value

unit

extra

```

`property` identifies the condition.

`value` is intentionally open-ended.

`unit` is optional.

`extra` permits experimental extensions.

---

### 6.5 Open condition values

This RFC deliberately does not require all conditions to be numeric.

Examples:

```text

concentration = 0.001 mol/L

dilution = "1/1000"

carrier = "air"

delivery\_method = "headspace"

temperature = 25 C

```

A future condition vocabulary or quantity specification MAY define stronger

semantics.

---

### 6.6 Missing condition

A missing condition MUST NOT be interpreted as zero.

For example:

```text

missing concentration != concentration 0

```

Similarly:

```text

missing dilution != zero dilution

```

Missing means that the condition is not represented.

---

### 6.7 Burton missing concentration evidence

The Burton experimental conversion contains:

```text

227 stimuli

225 with conditions

2 without conditions

```

The two stimuli without represented concentration are preserved without

inventing numeric values.

This demonstrates why concentration cannot be a mandatory Stimulus field.

---

## 7. Observation Target Model

### 7.1 Possible observation targets

Examples include:

```text

human participant

animal

olfactory receptor

glomerulus

region of interest

electronic sensor

sensor array

computational model

```

Future OpenSmell specifications MAY define more specialized target models.

---

### 7.2 ObservationTarget structure

Experimental conceptual structure:

```json

{

  "id": "...",

  "identifiers": [

    {

      "scheme": "dataset.subject",

      "value": "42"

    }

  ],

  "extra": {}

}

```

---

### 7.3 Target identity

An ObservationTarget is an OpenSmell resource.

It therefore has its own Resource ID.

Source dataset identifiers SHOULD be preserved separately through

`ExternalIdentifier`.

---

### 7.4 Burton target evidence

The Burton experiment produced:

```text

1,008 ObservationTarget resources

```

These represent source ROI/target identifiers.

The source metadata contained four identifiers referenced by physiological

observations but absent from the declared target metadata.

The source also contained four declared target identifiers that were not

referenced by observations.

The experimental model preserved both situations.

---

### 7.5 Source inconsistency and graph representation

The Burton source contained four referenced target identifiers absent from the

source target metadata:

```text

113L\_038

113L\_039

113R\_064

113R\_077

```

Rather than discarding 748 observations or inventing biological metadata, the

experimental adapter created target resources preserving those source

identifiers.

This distinction is important:

```text

unresolved in source metadata

```

does not necessarily mean:

```text

unresolved in OpenSmell resource graph

```

An adapter MAY create a minimal resource representing the unresolved source

identity.

---

## 8. Observation Model

### 8.1 Observation fields

The experimental Observation model contains:

```text

id

stimulus

target

results

context

identifiers

extra

```

---

### 8.2 Required stimulus reference

An Observation currently requires a Stimulus reference.

Conceptually:

```text

Observation -> Stimulus

```

This ensures that the observation identifies what was presented or applied.

Whether future OpenSmell versions should permit observations without a

Stimulus remains an open question.

---

### 8.3 Optional target reference

An Observation target is optional.

Some datasets or future use cases may not expose an identifiable target.

Therefore:

```text

target = absent

```

is structurally representable.

A scheme or application MAY impose stronger requirements.

---

### 8.4 Observation context

`context` contains observation-level contextual information that is not part

of the generic graph structure.

Experimental examples include:

```json

{

  "measurement\_domain": "psychophysical"

}

```

or:

```json

{

  "measurement\_domain": "physiological"

}

```

These values are currently experimental.

This RFC does not define a normative context vocabulary.

---

### 8.5 Observation results

An Observation contains:

```text

results[]

```

Each entry is a `Result`.

An Observation MAY contain:

```text

zero results

one result

multiple results

```

A scheme, dataset adapter, or application MAY impose stronger requirements.

---

## 9. Result Model

### 9.1 Why Result exists

Early experiments used a universal `Measurement` class directly inside

Observation.

That approach appeared attractive because many datasets contain numeric

measurements.

However, the model quickly encountered semantically different data:

```text

detection state

recognition response

psychophysical rating

physiological DeltaF

future sensor output

future model output

```

These are not necessarily instances of one universal measurement concept.

The universal Measurement abstraction was therefore removed from the generic

resource graph.

---

### 9.2 Result fields

The experimental Result model contains:

```text

scheme

data

extra

```

where:

- `scheme` identifies the interpretation rules and their version;

- `data` contains scheme-defined data;

- `extra` preserves additional information.

---

### 9.3 Result scheme

A Result scheme defines how `data` should be interpreted.

The current experimental model represents it as:

```text
ResultScheme
├── id
├── version
└── extra
```

The pair `scheme.id` + `scheme.version` identifies the interpretation rules
expected for `Result.data`.

The generic Observation model MUST NOT attempt to interpret arbitrary Result
data without knowledge of the scheme identifier and applicable scheme version.

### 9.4 Unknown Result schemes

Unknown Result schemes MUST remain representable.

Example:

```json
{
  "scheme": {
    "id": "vendor.example.future-result",
    "version": "7.2"
  },
  "data": {
    "something": 123
  }
}
```

A structurally valid Result MUST NOT become invalid merely because a consumer
does not recognize `scheme.id` or `scheme.version`.

This is required for forward compatibility.

### 9.5 Scheme-specific validation

Generic structural validation and scheme-specific validation are separate.

Generic validation may verify:

```text

scheme is a ResultScheme

scheme.id is a non-empty string

scheme.version is a non-empty string

scheme.extra is an object

data is an object

```

A scheme-specific validator may then impose additional requirements.

For example, a perceptual measurement scheme may require:

```text

measurements

property

numeric value

scale

```

while a biological scheme may define entirely different fields.

---

### 9.6 Multiple Result schemes

An Observation MAY contain Results from multiple schemes.

Example:

```text

Observation

|

+-- Result

\|   scheme = categorical observation scheme

|

+-- Result

    scheme = perceptual measurement scheme

```

This was required by the Keller/Vosshall experiment.

---

### 9.7 Zero is data

Numeric zero MUST be preserved when explicitly present.

For example:

```json

{

  "property": "delta\_f",

  "value": 0.0

}

```

contains an explicit value.

It MUST NOT be treated as equivalent to:

```text

missing measurement

```

---

### 9.8 Negative zero

Source datasets may contain IEEE-754 negative zero:

```text

-0.0

```

The Burton source produced such a value in the experimental conversion.

The current experiment preserves it.

This RFC does not currently require canonicalization:

```text

-0.0 -> 0.0

```

Whether numeric canonicalization should occur during serialization is outside

the scope of this RFC.

---

### 9.9 Missing Result

An Observation with no Result is structurally representable by the current

experimental model.

This does not mean that such an Observation is scientifically useful.

Possible reasons include:

- incomplete dataset;

- incremental graph construction;

- result stored separately;

- failed measurement;

- unknown result;

- future workflow requirements.

Future schemes MAY require one or more Results.

---

### 9.10 Missing Result is not zero

The following distinction MUST be preserved:

```text

no result

```

is not equivalent to:

```text

result value = 0

```

Similarly:

```text

no measurement

```

is not equivalent to:

```text

measurement value = 0

```

---

### 9.11 Result versus Representation

`Result` and OpenSmell Core `Representation` have related extensibility

patterns but different semantic roles.

A Representation describes an odor representation.

A Result describes the outcome of an Observation.

Conceptually:

```text

Odor

└── Representation

Observation

└── Result

```

They MUST NOT be treated as interchangeable merely because both use schemes.

---

### 9.12 Scheme architecture

Both concepts follow the architectural principle:

```text

stable generic container

\+

scheme-defined semantics

```

This reduces pressure to continuously expand the generic OpenSmell data model

whenever a new scientific modality appears.

---

## 10. Keller/Vosshall Human Psychophysics Experiment

### 10.1 Evidence from Keller/Vosshall

The Keller/Vosshall experimental dataset contains:

```text

480 unique PubChem CIDs

960 unique molecule+dilution stimuli

```

The experimental OpenSmell adapter therefore produced:

```text

480 molecule identities

960 Stimulus resources

```

This demonstrates that molecule identity and stimulus identity cannot safely be

treated as equivalent.

---

### 10.2 Keller/Vosshall categorical Result

The experimental Keller/Vosshall adapter represented detection and recognition

using an experimental categorical Result scheme:

```text

org.opensmell.experimental.observation.categories
version 0.1

```

Example conceptual data:

```json

{

  "detection": "detected",

  "recognition": "I don't know what the odor is"

}

```

This scheme is experimental.

Its appearance in the prototype MUST NOT be interpreted as reserving or

standardizing this scheme identifier.

---

### 10.3 Keller/Vosshall perceptual Result

Quantitative psychophysical measurements were represented separately using:

```text

org.opensmell.perceptual.measurements
version 0.1

```

Example:

```json

{

  "measurements": [

    {

      "property": "intensity",

      "value": 59.0,

      "scale": {

        "min": 0,

        "max": 100

      }

    }

  ]

}

```

The semantics of this quantitative perceptual model belong to RFC-0005.

RFC-0007 does not redefine them.

---

### 10.4 Detection is not a quantitative rating

Detection state and quantitative perceptual rating are deliberately separate.

For example:

```text

detected

```

is not equivalent to:

```text

intensity = 1

```

Similarly:

```text

not detected

```

is not equivalent to:

```text

intensity = 0

```

The absence of a quantitative rating MUST NOT automatically be interpreted as

a zero rating.

---

### 10.5 Keller/Vosshall evidence

The experimental Keller/Vosshall graph produced:

```text

Source rows:                  55,000

Molecule identities:             480

Stimulus resources:              960

Observation targets:              55

Observations:                  55,000

```

Detection:

```text

Detected:                     41,289

Not detected:                 13,711

Unknown:                           0

```

Results:

```text

Categorical Results:          55,000

Perceptual Results:           41,289

Total Result objects:         96,289

Recognition values:           41,289

Numeric measurements:        263,683

```

The 41,289 detected observations corresponded exactly to the observations

containing explicit quantitative psychophysical measurements.

The 13,711 non-detected observations contained no explicit quantitative

psychophysical measurements.

---

### 10.6 Keller/Vosshall identity evidence

The experimental graph contained:

```text

56,495 Resource IDs

56,495 unique Resource IDs

0 collisions

```

The resources consisted of:

```text

480 molecule identities

960 stimuli

55 observation targets

55,000 observations

```

This demonstrates that the resource model can coexist with the deterministic

identity experiment defined by RFC-0006.

---

## 11. Burton 2022 Mouse Physiology Experiment

### 11.1 Evidence from Burton 2022

The Burton dataset contains:

```text

186 molecules

227 stimuli

```

Again:

```text

Molecule != Stimulus

```

Some molecules occur in more than one stimulus configuration.

This independently supports the same architectural distinction observed in the

Keller/Vosshall dataset.

---

### 11.2 Burton physiological Result

The Burton experiment represented physiological DeltaF responses using an

experimental result scheme:

```text

org.opensmell.experimental.biological.measurements
version 0.1

```

Conceptual example:

```json

{

  "scheme": {
    "id": "org.opensmell.experimental.biological.measurements",
    "version": "0.1"
  },

  "data": {

    "measurements": [

      {

        "property": "delta\_f",

        "value": 0.0

      }

    ]

  }

}

```

This scheme is not normative.

This RFC does not define a general biological measurement specification.

---

### 11.3 Burton evidence

The experimental Burton graph produced:

```text

Molecule identities:             186

Stimulus resources:              227

Observation targets:           1,008

Observations:                 187,748

Total resources:             189,169

```

Stimuli:

```text

With resolved source:             226

Without resolved source:            1

With conditions:                   225

Without conditions:                  2

```

Results:

```text

Result objects:                187,748

DeltaF values preserved:       187,748

Zero DeltaF:                   184,356

Non-zero DeltaF:                 3,392

```

Identity:

```text

189,169 Resource IDs

189,169 unique Resource IDs

0 collisions

```

---

## 12. UCI Electronic Olfaction Experiment

The UCI *Gas Sensor Array Drift at Different Concentrations* dataset was used as a third independent stress test of the RFC-0007 architecture. The source dataset contains 13,910 measurements from a 16-sensor chemical sensor array exposed to six gases at varying concentrations. Each source measurement contains 128 extracted values: eight features for each of the 16 sensor channels.

### 12.1 Modeling decision

One source row is modeled as one `Observation`. A unique analyte/concentration pair is modeled as one `Stimulus`. The complete 16-sensor array is modeled as one composite `ObservationTarget`, rather than inventing 16 independent observations for a source row that represents one array measurement. This experiment therefore demonstrates that an `ObservationTarget` MAY represent a composite observation system.

### 12.2 Result structure

Each observation contains one experimental sensor-array `Result` using the scheme identifier:

```text
org.opensmell.experimental.sensor-array.features
version 0.1
```

The result preserves 16 sensor channels, with eight extracted features per channel. The generic RFC-0007 `Result` model required no change to represent this nested electronic-olfaction data.

### 12.3 Preservation results

The experiment processed:

```text
Source measurements :     13,910
Unique stimuli      :        233
Observation targets :          1
Observations         :     13,910
Result objects       :     13,910
Feature values       :  1,780,480
```

All 1,780,480 feature values were reconstructed and compared with the source values. No value mismatch was observed.

The source values included:

```text
Negative values :   668,434
Zero values     :        55
Positive values : 1,111,991
```

The experiment therefore provides additional evidence that zero and negative numeric values must remain data and must not be conflated with missingness.

### 12.4 Source identity discipline

The generated composite sensor-array target is an OpenSmell modeling abstraction and is not presented as a source-native UCI identifier. Generated analyte identities are likewise kept distinct from source-native identifiers. This follows RFC-0006 and the RFC-0007 rule that adapters MUST NOT invent external identifiers and present them as if they came from the source dataset.

### 12.5 Architectural evidence

The electronic-olfaction experiment supports the same generic graph used by the human psychophysics and mouse physiology experiments:

```text
Stimulus
   |
   v
Observation -----> ObservationTarget
   |
   v
Result(s)
```

No new generic RFC-0007 resource class was required. In particular, the experiment supports the conclusions that `Stimulus != Observation`, an observation target can be composite, and domain-specific nested result structures belong in scheme-defined `Result.data` rather than in a universal measurement structure.

## 13. Resource Graph and Identity

### 13.1 Resource graph

The proposed model naturally forms a directed graph.

Example:

```text

Molecule M1

    ^

    |

Stimulus S1

    ^

    |

Observation O1 ----> Target T1

    |

    +---- Result R1

```

Multiple observations may reference the same Stimulus.

Multiple stimuli may reference the same source resource.

Multiple observations may reference the same target.

---

### 13.2 Resource reuse

Resources SHOULD be reusable through references.

For example:

```text

Molecule M1

   ^

   |

   +---- Stimulus S1

   |

   +---- Stimulus S2

```

and:

```text

Stimulus S1

   ^

   |

   +---- Observation O1

   |

   +---- Observation O2

   |

   +---- Observation O3

```

This avoids unnecessary duplication.

---

### 13.3 Resource identity

Resource identity SHOULD follow the principles developed in RFC-0006.

Those principles include:

- stability;

- uniqueness within the intended scope;

- opacity;

- independence from external systems;

- referenceability;

- round-trip preservation.

RFC-0007 does not redefine Resource ID syntax.

---

### 13.4 Experimental UUID generation

Current experiments use deterministic UUIDv5 Resource IDs for imported dataset

resources.

Conceptually:

```text

dataset

\+

resource type

\+

canonical source identity

        |

        v

      UUIDv5

```

This generation mechanism belongs to RFC-0006.

RFC-0007 depends on resource identity but does not standardize UUID generation.

---

### 13.5 Resource type and identity

Resource type participates in the current experimental deterministic identity

generation.

Therefore identical source strings used for different resource types do not

necessarily identify the same OpenSmell resource.

For example:

```text

source identity "42" as target

```

and:

```text

source identity "42" as stimulus

```

are different resource identities.

---

### 13.6 Composite source identity

Some resources require composite source identity.

For example, Keller/Vosshall Stimulus identity currently uses:

```json

{

  "cid": "240",

  "dilution": "1/1000"

}

```

This is appropriate because neither CID nor dilution alone uniquely identifies

the stimulus configuration.

---

### 13.7 Observation identity

Observation identity may also require multiple source fields.

The Keller/Vosshall experiment currently uses:

```json

{

  "cid": "...",

  "dilution": "...",

  "occurrence": "...",

  "subject": "..."

}

```

This is an adapter-level experimental choice.

It MUST NOT be interpreted as a normative OpenSmell observation identity

algorithm.

---

### 13.8 Keller/Vosshall occurrence limitation

The current Keller/Vosshall adapter uses an occurrence counter to distinguish

repeated observations sharing:

```text

subject

CID

dilution

```

The occurrence counter depends on source row ordering.

Therefore, reordering the source dataset may alter generated observation IDs.

This is a known limitation.

RFC-0007 does not standardize occurrence-based identity.

A future adapter SHOULD prefer a stable source-native observation identifier

when one is available and semantically justified.

---

### 13.9 Identity stability requirement

A future normative resource model SHOULD distinguish:

```text

resource model

```

from:

```text

dataset-specific identity extraction strategy

```

The generic resource model should not require knowledge of how every source

dataset determines observation identity.

---

### 13.10 Identity is not equality

Two resources containing identical data are not automatically the same

resource.

For example:

```text

Observation A:

intensity = 50

Observation B:

intensity = 50

```

does not imply:

```text

Observation A == Observation B

```

They may correspond to different participants, trials, stimuli, times, or

experimental contexts.

---

### 13.11 Content is not identity

Resource IDs MUST NOT be treated merely as hashes of current resource

contents.

Resource metadata may evolve while resource identity remains stable.

This principle is inherited from RFC-0006.

---

## 14. Serialization and Forward Compatibility

### 14.1 Serialization

This RFC does not yet define a normative JSON serialization for the complete

resource graph.

The JSON fragments in this document are conceptual examples.

Future work must determine whether resources are serialized:

- inside one OpenSmell document;

- as separate resources;

- as collections;

- through linked documents;

- through an API;

- through another transport format.

---

### 14.2 Experimental use through extensions

Because OpenSmell 0.1 permits extension fields in relevant locations,

experimental resource graphs may be explored without immediately changing the

Core specification.

However, extension use MUST NOT be mistaken for normative adoption.

---

### 14.3 Forward compatibility

Future OpenSmell resource models SHOULD preserve the general principle that

unknown extension data and unknown schemes can survive parsing and

serialization when structurally possible.

This is consistent with the forward-compatible design of OpenSmell Core

representations.

---

## 15. Scheme Architecture

### 15.1 Scheme ownership

This RFC defines the Result mechanism.

It does not automatically define the schemes used by experimental adapters.

In particular:

```text

org.opensmell.experimental.observation.categories

org.opensmell.experimental.biological.measurements

```

are prototype identifiers.

They are not normative registrations.

---

### 15.2 Perceptual measurement scheme

The identifier:

```text

org.opensmell.perceptual.measurements

```

is associated with the experimental quantitative perceptual model described by

RFC-0005.

RFC-0007 only defines how such scheme-defined data may participate in an

Observation Result.

---

### 15.3 Result scheme registry

A future OpenSmell version MAY introduce a Result scheme registry similar in

purpose to the Representation scheme registry.

Such a registry could associate:

```text

scheme identifier

version

validator

expected data model

documentation

```

This RFC does not define that registry.

---

### 15.4 Result scheme versioning

The current experimental `Result` structure contains:

```text
scheme
data
extra
```

`scheme` is a structured `ResultScheme` containing:

```text
id
version
extra
```

Versioning is therefore explicit in the current RFC-0007 prototype.

Conceptual form:

```json
{
  "scheme": {
    "id": "org.opensmell.experimental.sensor-array.features",
    "version": "0.1"
  },
  "data": {}
}
```

A consumer that recognizes a scheme identifier but not its declared version
MUST NOT assume that it understands the payload semantics.

Unknown scheme identifiers and unknown scheme versions SHOULD remain
representable and preservable when structurally valid.

This is an experimental design decision and does not modify OpenSmell 0.1 Core.

### 15.5 Reusing Core Scheme

OpenSmell Core already defines a `Scheme` structure containing:

```text
id
version
extra
```

The current RFC-0007 prototype deliberately does not reuse that class.

Instead, `src/opensmell/experimental/resources.py` defines `ResultScheme` with
the same current field shape.

This provides explicit versioning without prematurely coupling Result scheme
lifecycle, validation, or future registration rules to Core Representation
schemes.

Future work MAY unify `ResultScheme` and Core `Scheme` if evidence shows that
they should share one abstraction.

This remains an open design question.

## 16. Conditions, Context, and Provenance

### 16.1 Conditions versus context

Stimulus `conditions` and Observation `context` serve different purposes.

Conceptually:

```text

Stimulus.conditions

```

describes how something was presented or applied.

```text

Observation.context

```

describes circumstances associated with the observation or result.

The exact boundary may require refinement.

---

### 16.2 Example distinction

Possible Stimulus conditions:

```text

concentration

dilution

temperature

carrier

flow

delivery duration

```

Possible Observation context:

```text

protocol

measurement domain

session

aggregation method

instrument configuration

```

These examples are informative, not normative vocabularies.

---

### 16.3 Provenance

Scientific observation data frequently requires provenance.

Possible provenance includes:

```text

dataset

publication

instrument

protocol

research group

processing pipeline

model

software version

```

RFC-0003 is expected to address provenance concepts.

RFC-0007 does not duplicate a complete provenance model.

---

### 16.4 Result provenance

A future design may require provenance at different levels:

```text

Observation

Result

Stimulus

source resource

```

This RFC does not determine which provenance fields belong at which level.

---

## 17. Dataset Adapter Requirements

### 17.1 Dataset adapters

Dataset adapters are responsible for mapping source semantics into OpenSmell

resources.

Adapters SHOULD:

- preserve source-native identifiers;

- preserve explicit zero values;

- preserve missingness;

- avoid inventing scientific meaning;

- distinguish source resources from stimuli;

- distinguish observations from results;

- report unresolved references when useful.

---

### 17.2 Adapter-specific logic

Some logic necessarily remains dataset-specific.

Examples include:

```text

which source columns define stimulus identity

which source columns define observation identity

which values mean missing

which columns contain measurements

which identifiers are source-native

```

RFC-0007 MUST NOT attempt to standardize every dataset adapter.

---

### 17.3 Missingness

Missingness is a first-class semantic concern.

The following distinctions SHOULD be preserved:

```text

missing source

missing condition

missing target

missing result

explicit zero

explicit empty string

unknown state

not detected

```

These values MUST NOT be collapsed without scheme- or dataset-specific

justification.

---

### 17.4 Explicit absence

Some schemes may define explicit absence states.

For example:

```text

detection = not\_detected

```

is explicit data.

It is different from:

```text

detection field missing

```

Similarly, RFC-0004 distinguishes explicit semantic absence from missing

annotation.

---

### 17.5 Open data payload

`Result.data` is intentionally an open object.

This provides flexibility but reduces generic semantic validation.

This trade-off is deliberate during the experimental phase.

A future normative specification may impose additional constraints.

---

## 18. Rejected Alternatives

### 18.1 Why not one universal Result schema

Olfactory observations can span multiple scientific domains.

A universal schema would need to simultaneously model:

```text

human perception

animal physiology

receptor response

sensor signals

chemical analysis

model predictions

categorical outcomes

time series

multidimensional vectors

```

Attempting to standardize all of these inside the generic Observation resource

would make the base model complex and brittle.

Scheme-defined Result data avoids this problem.

---

### 18.2 Why not store everything in extra

Using only arbitrary extension fields would technically permit the same data,

but would provide little interoperability.

The explicit `Result` abstraction provides a stable extension point:

```text

Observation

    results[]

        scheme

        data

```

Applications can therefore discover the interpretation mechanism without

understanding every possible result type.

---

### 18.3 Why not put results directly in Observation

A flat Observation structure such as:

```json

{

  "detection": "...",

  "intensity": 42,

  "delta\_f": 0.3

}

```

would mix unrelated scientific domains and make future extension difficult.

The Result abstraction isolates domain semantics.

---

## 19. Experimental Python Model

### 19.1 Experimental Python model

The current prototype contains conceptual Python structures equivalent to:

```python

@dataclass(frozen=True)

class Reference:

    resource\_id: str



@dataclass(frozen=True)

class ExternalIdentifier:

    scheme: str

    value: str



@dataclass

class Condition:

    property: str

    value: Any

    unit: str | None = None

    extra: dict[str, Any] = field(default\_factory=dict)



@dataclass(frozen=True)
class ResultScheme:
    id: str
    version: str
    extra: dict[str, Any] = field(default_factory=dict)


@dataclass
class Result:
    scheme: ResultScheme
    data: dict[str, Any] = field(default_factory=dict)
    extra: dict[str, Any] = field(default_factory=dict)


@dataclass

class Stimulus:

    id: str

    source: Reference | None = None

    identifiers: list[ExternalIdentifier] = field(default\_factory=list)

    conditions: list[Condition] = field(default\_factory=list)

    extra: dict[str, Any] = field(default\_factory=dict)



@dataclass

class ObservationTarget:

    id: str

    identifiers: list[ExternalIdentifier] = field(default\_factory=list)

    extra: dict[str, Any] = field(default\_factory=dict)



@dataclass

class Observation:

    id: str

    stimulus: Reference

    target: Reference | None = None

    results: list[Result] = field(default\_factory=list)

    context: dict[str, Any] = field(default\_factory=dict)

    identifiers: list[ExternalIdentifier] = field(default\_factory=list)

    extra: dict[str, Any] = field(default\_factory=dict)

```

This code is informative.

The RFC defines concepts, not a mandatory Python implementation.

---

### 19.2 Experimental validation

The prototype currently validates only basic structural requirements.

Examples include:

```text

Resource IDs are non-empty strings.

Reference Resource IDs are non-empty strings.

External identifier scheme/value are non-empty strings.

Condition property is non-empty.

Result scheme is a `ResultScheme`.

Result scheme ID is non-empty.

Result scheme version is non-empty.

Result scheme extra is an object.

Result data is an object.

```

Stronger validation belongs to future specifications and scheme-specific

validators.

---

### 19.3 Resource ID validation separation

The experimental resource classes deliberately do not currently enforce the

RFC-0006 UUID candidate syntax.

This prevents premature coupling between:

```text

resource structure

```

and:

```text

experimental identity syntax

```

An implementation may combine both validators when appropriate.

---

### 19.4 Graph validation

Graph validation MAY perform checks beyond structural resource validation.

Examples include:

```text

reference target exists

Resource IDs are unique

Stimulus source resolves

Observation stimulus resolves

Observation target resolves

scheme is supported

```

Failure of one graph-level check does not necessarily imply that the

individual resource is structurally invalid.

---

## 20. Experimental Evidence Summary

### 20.1 Python test suite

The current experimental resource model is covered by unit tests for references, external identifiers, conditions, Results, unknown Result schemes, zero and negative-zero preservation, Stimuli, source-less Stimuli, ObservationTargets, Observations, optional targets, multiple Result schemes, unresolved references, empty Result lists, and arbitrary extension data.

At the time of this RFC draft, the complete OpenSmell test suite reports:

```text
236 passed
```

with one environment-specific pytest cache warning on the development Windows system. The cache warning is unrelated to OpenSmell model validation.

### 20.2 Cross-domain validation

The same generic RFC-0007 architecture has been exercised against three substantially different scientific domains:

| Dataset | Domain | Observations | Result semantics |
| --- | --- | ---: | --- |
| Burton 2022 | mouse olfactory-bulb physiology | 187,748 | physiological DeltaF |
| Keller/Vosshall | human psychophysics | 55,000 | categorical and quantitative perceptual results |
| UCI Gas Sensor Array Drift | electronic olfaction | 13,910 | 16-channel sensor-array features |

All three use the same generic relationship:

```text
Stimulus
   |
   v
Observation -----> ObservationTarget
   |
   v
Result(s)
```

### 20.3 Total experimental observation count

The three experiments contain:

```text
Burton observations:             187,748
Keller/Vosshall observations:     55,000
UCI sensor observations:          13,910
                                 -------
Total:                           256,658
```

This does not prove universal applicability. It provides substantially stronger evidence than synthetic examples or a single scientific domain alone.

### 20.4 Total Result count

Current experiments produce:

```text
Burton Results:                  187,748
Keller categorical Results:       55,000
Keller perceptual Results:        41,289
UCI sensor-array Results:         13,910
                                 -------
Total Results:                   297,947
```

These Results span physiological, categorical, quantitative perceptual, and electronic sensor data.

### 20.5 Electronic-olfaction value preservation

The UCI experiment preserves all 128 extracted features for each of 13,910 source measurements:

```text
Total feature values: 1,780,480
Negative values:        668,434
Zero values:                 55
Positive values:       1,111,991
Mismatches:                    0
```

This provides direct evidence that scheme-defined `Result.data` can preserve dense nested sensor data without changing the generic RFC-0007 model.

---

## 21. Relationships to Other OpenSmell Work

### 21.1 Relationship to RFC-0003

RFC-0003 explores provenance.

RFC-0007 expects provenance to become important for experimental resources but

does not redefine it.

Future work should determine how provenance attaches to:

```text

Stimulus

Observation

Result

ObservationTarget

```

---

### 21.2 Relationship to RFC-0004

RFC-0004 defines an experimental semantic annotation model.

Semantic annotation and Observation Result are distinct concepts.

For example:

```text

Odor semantic annotation:

    floral = present

```

does not necessarily describe:

```text

a participant's response during one experimental trial

```

Dataset adapters must preserve this distinction.

---

### 21.3 Relationship to RFC-0005

RFC-0005 defines quantitative perceptual measurements.

RFC-0007 provides an Observation architecture capable of carrying RFC-0005

data as a Result.

Conceptually:

```text

Observation

└── Result

    ├── scheme = org.opensmell.perceptual.measurements

    └── data = RFC-0005 measurement data

```

RFC-0007 does not redefine RFC-0005 measurement semantics.

---

### 21.4 Relationship to RFC-0006

RFC-0006 explores resource identification.

RFC-0007 relies on the concept of Resource IDs and references.

Current experimental dataset adapters use RFC-0006 deterministic Resource ID

generation.

However:

```text

RFC-0007 resource structure

```

and:

```text

RFC-0006 identity generation

```

remain separable concerns.

---

### 21.5 Relationship to OpenSmell 0.1

OpenSmell 0.1 remains focused on the representation of an Odor.

RFC-0007 is a candidate extension for a future OpenSmell version.

Nothing in this RFC changes conformance requirements for OpenSmell 0.1.

---

### 21.6 Relationship to rendering

A Stimulus is not a rendering command.

For example:

```text

Stimulus:

    molecule X

    concentration Y

```

does not tell a diffuser how to physically reproduce that stimulus.

Rendering remains a separate concern.

OpenSmell MUST NOT assume how an odor or stimulus is physically reproduced.

---

## 22. Security, Privacy, Scientific Integrity, and Licensing

### 22.1 Security considerations

Resource graphs may contain externally supplied identifiers and arbitrary

scheme-defined data.

Implementations SHOULD treat these values as untrusted input.

Implementations SHOULD NOT:

- execute arbitrary Result data;

- interpret identifiers as filesystem paths without validation;

- construct executable queries without escaping;

- assume scheme identifiers are trustworthy merely because they use an

  OpenSmell-like namespace.

---

### 22.2 Privacy considerations

Observation datasets may contain information associated with human

participants.

External subject identifiers may be sensitive or indirectly identifying.

OpenSmell serialization does not make such data safe to publish.

Implementations and dataset publishers are responsible for:

- appropriate anonymization;

- consent;

- access control;

- applicable privacy law;

- dataset licensing;

- ethical requirements.

OpenSmell Resource IDs MUST NOT be assumed to provide anonymization by

themselves.

---

### 22.3 Scientific integrity considerations

Adapters MUST avoid inventing unsupported scientific meaning.

For example, an adapter SHOULD NOT automatically interpret:

```text

source missing

```

as:

```text

control

```

unless the source dataset or documentation supports that interpretation.

Likewise:

```text

missing measurement

```

MUST NOT automatically become:

```text

measurement = 0

```

---

### 22.4 Licensing considerations

The OpenSmell resource model does not alter the license of imported datasets.

Adapters and examples MUST respect the license and redistribution terms of

their source datasets.

OpenSmell MUST NOT imply ownership of imported scientific data.

---

## 23. Interoperability Principles and Design Invariants

### 23.1 Interoperability principle

The objective of RFC-0007 is not to create a universal ontology for every

olfactory experiment.

The objective is to provide a stable interoperability layer:

```text

resource identity

\+

references

\+

Stimulus

\+

ObservationTarget

\+

Observation

\+

scheme-defined Result

```

while allowing domain-specific semantics to evolve independently.

---

### 23.2 Design invariant 1

An Odor MUST NOT be assumed to be a Stimulus.

---

### 23.3 Design invariant 2

A Stimulus MUST NOT be assumed to be an Observation.

---

### 23.4 Design invariant 3

An ObservationTarget MUST NOT be assumed to be human.

---

### 23.5 Design invariant 4

An Observation MUST reference a Stimulus in the current experimental model.

---

### 23.6 Design invariant 5

An Observation MAY reference an ObservationTarget.

---

### 23.7 Design invariant 6

A Stimulus MAY reference a source resource.

---

### 23.8 Design invariant 7

A missing Stimulus source MUST NOT automatically imply a control stimulus.

---

### 23.9 Design invariant 8

A Stimulus MAY contain zero or more Conditions.

---

### 23.10 Design invariant 9

A missing Condition MUST NOT be interpreted as zero.

---

### 23.11 Design invariant 10

An Observation MAY contain multiple Results.

---

### 23.12 Design invariant 11

Result semantics are defined by the Result scheme.

---

### 23.13 Design invariant 12

Unknown Result schemes SHOULD be preservable.

---

### 23.14 Design invariant 13

The generic Observation model MUST NOT require one universal measurement

structure.

---

### 23.15 Design invariant 14

Explicit numeric zero MUST remain distinguishable from missing data.

---

### 23.16 Design invariant 15

External identifiers MUST remain conceptually distinct from OpenSmell Resource

IDs.

---

### 23.17 Design invariant 16

Adapters SHOULD preserve source-native identifiers rather than inventing

external identifiers.

---

### 23.18 Design invariant 17

Unresolved references SHOULD be preservable.

---

### 23.19 Design invariant 18

Structural validation SHOULD remain separable from graph resolution.

---

### 23.20 Design invariant 19

Resource identity MUST NOT be inferred solely from current resource content.

---

### 23.21 Design invariant 20

Result and Representation MUST remain semantically distinct.

---

### 23.22 Design invariant 21

RFC-0007 MUST NOT prescribe how an odor is physically reproduced.

---

## 24. Open Questions

### 24.1 Resolved experimental decision: Result scheme version

The current RFC-0007 prototype uses:

```json
{
  "scheme": {
    "id": "...",
    "version": "..."
  }
}
```

rather than a plain scheme string.

The Python model represents this structure with `ResultScheme`.

This decision is supported by successful validation across Keller/Vosshall
human psychophysics, Burton 2022 mouse physiology, and UCI electronic
olfaction, using four Result scheme/version pairs.

The decision remains experimental until RFC-0007 is promoted.

### 24.2 Open question 2: Core Scheme reuse

Should the experimental `ResultScheme` eventually reuse or unify with the existing Core `Scheme` structure?

---

### 24.3 Open question 3: Observation without Stimulus

Should future OpenSmell versions permit an Observation without a Stimulus

reference?

---

### 24.4 Open question 4: Empty Observation

Should an Observation with zero Results remain valid in a future normative

specification?

---

### 24.5 Open question 5: Target taxonomy

Should OpenSmell define standard target categories such as:

```text

human

animal

receptor

roi

sensor

model

```

or leave target classification entirely scheme-defined?

---

### 24.6 Open question 6: Condition vocabulary

Should common conditions such as:

```text

concentration

dilution

temperature

flow

duration

carrier

```

receive standardized identifiers?

---

### 24.7 Open question 7: Units

Should OpenSmell define a unit representation or reuse an existing unit

standard?

---

### 24.8 Open question 8: Graph serialization

How should resource graphs be serialized?

Possible approaches include:

```text

one document containing resource collections

linked OpenSmell documents

JSON-LD-like graph structure

API resources

streamed records

```

No decision is made.

---

### 24.9 Open question 9: Result registry

Should Result schemes use the existing scheme registry infrastructure or a

separate registry?

---

### 24.10 Open question 10: Observation identity

How should adapters derive stable Observation identity when a dataset does not

provide a native observation identifier?

---

### 24.11 Open question 11: Trial identity

Should OpenSmell introduce a separate `Trial` or `Presentation` resource

between Stimulus and Observation?

For example:

```text

Stimulus

   |

   v

Trial

   |

   v

Observation

```

The current datasets do not yet justify standardizing this additional layer.

---

### 24.12 Open question 12: Time

Should observation time be represented directly on Observation, through

context, or through a future event model?

---

### 24.13 Open question 13: Result identity

Should individual Results eventually become independently addressable

resources with their own Resource IDs?

The current prototype embeds Results inside Observations.

---

### 24.14 Open question 14: Result ordering

Does the order of `results[]` carry meaning?

The current experimental model does not define ordering semantics.

A future normative specification SHOULD explicitly decide this.

---

### 24.15 Open question 15: Condition ordering

Does the order of `conditions[]` carry meaning?

The current model does not define ordering semantics.

---

### 24.16 Open question 16: Duplicate schemes

May one Observation contain multiple Results using the same scheme?

The generic experimental model currently permits this structurally.

Individual schemes or future graph rules may restrict it.

---

### 24.17 Open question 17: Embedded versus referenced Result

Should Results remain embedded inside Observation or become referenced

resources?

---

### 24.18 Open question 18: Dataset resource

Should imported observations reference an explicit Dataset resource?

This could improve provenance and identity scoping.

---

### 24.19 Open question 19: Protocol resource

Should experimental protocols become explicit resources referenced by

Stimulus or Observation?

---

### 24.20 Open question 20: Subject privacy

Should OpenSmell define recommendations for pseudonymous human participant

identifiers?

---

### 24.21 Open question 21: Numeric canonicalization

Should serialization normalize:

```text

-0.0

```

to:

```text

0.0

```

or preserve the source floating-point representation when possible?

---

### 24.22 Open question 22: Biological measurement scheme

Should physiological measurements eventually receive a dedicated OpenSmell

scheme?

The Burton experiment demonstrates the need for such data but does not provide

enough evidence to define a universal biological model.

---

### 24.23 Open question 23: Categorical observation scheme

Should detection and recognition receive a standardized observation scheme?

The Keller/Vosshall experiment demonstrates a useful category but one dataset

is insufficient to define universal semantics.

---

### 24.24 Open question 24: Sensor result scheme

What common abstractions, if any, should future electronic olfactory sensor

results share?

---

### 24.25 Open question 25: Observation aggregation

How should aggregate observations be distinguished from individual trials?

Examples include:

```text

individual participant rating

population mean

median response

model prediction

sensor average

```

RFC-0005 already identifies aggregation as important context for perceptual

measurements, but RFC-0007 does not yet define a generic aggregation model.

---

## 25. Implementation Roadmap and Promotion Criteria

### 25.1 Proposed direction

Future OpenSmell development SHOULD continue experimenting with this resource

model before promoting it into a normative specification.

Priority areas include:

1\. a third independent observation dataset;

2\. Result scheme versioning;

3\. stable observation identity;

4\. graph serialization;

5\. provenance integration;

6\. validation boundaries;

7\. sensor or model output experiments.

---

### 25.2 Acceptance criteria for future promotion

RFC-0007 SHOULD NOT become normative merely because the current Python

prototype works.

Before promotion, OpenSmell should demonstrate:

- stable serialization;

- cross-language interoperability;

- deterministic reference behavior;

- explicit scheme versioning;

- preservation of unknown schemes;

- preservation of missingness;

- graph validation rules;

- at least one additional independent data domain;

- documented privacy considerations;

- documented provenance behavior.

---

## 26. Current Implementation Status

At the time of this draft:

```text

src/opensmell/experimental/resources.py

```

contains the experimental resource classes, including `ResultScheme`.

The experimental dataset graph tools are:

```text

tools/analyze\_burton\_resource\_graph.py

tools/analyze\_keller\_vosshall\_resource\_graph.py

tools/analyze\_gas\_sensor\_resource\_graph.py

```

The experimental unit tests are:

```text

tests/test\_experimental\_resources.py

```

These files remain outside the OpenSmell 0.1 Core API contract.

---

## 27. Compatibility with OpenSmell 0.1

### 27.1 OpenSmell 0.1 compatibility

OpenSmell 0.1 does not define:

```text

Stimulus

Observation

ObservationTarget

Result

```

Therefore an OpenSmell 0.1 implementation is not required to support this RFC.

Experimental implementations SHOULD keep these concepts outside the 0.1 Core

schema until a future specification explicitly adopts them.

---

### 27.2 Compatibility statement

Implementations conforming only to OpenSmell 0.1 MAY completely ignore

RFC-0007.

Implementations experimenting with RFC-0007 MUST NOT claim that RFC-0007

resources are required for OpenSmell 0.1 conformance.

---

## 28. Current Conclusion

### 28.1 Experimental conclusion

The current evidence supports the following generic architecture:

```text

Source resource

      |

      v

   Stimulus

      |

      v

 Observation -----> ObservationTarget

      |

      v

   Result(s)

      |

      v

scheme-defined semantics

```

The architecture successfully represented both:

```text

mouse physiological observations

```

and:

```text

human psychophysical observations

```

without requiring those domains to share one universal measurement class.

The UCI electronic-olfaction stress test further showed that the same generic
architecture supports composite sensor-array output with 1,780,480 feature
values preserved exactly.

The current prototype also demonstrated explicit Result scheme versioning
without changing the generic Stimulus, ObservationTarget, Observation, or
resource-identity models.

---

### 28.2 Final principle

OpenSmell should standardize the boundaries between olfactory information

systems without pretending that every scientific domain measures smell in the

same way.

The proposed resource graph therefore standardizes the relationship:

```text

what was presented

        |

        v

what was observed

        |

        v

how that result is interpreted

```

rather than attempting to standardize every possible observation result inside

one universal structure.

## References

### OpenSmell RFC-0003

OpenSmell provenance model.

### OpenSmell RFC-0005

Quantitative Perceptual Measurement Model.

### OpenSmell RFC-0006

Resource Identification and Deterministic Source Identity.

### Keller/Vosshall olfactory perception dataset

Human psychophysical observations used to validate categorical and quantitative perceptual results.

### Burton 2022 / Pyrfume dataset

Mouse olfactory-bulb physiological observations used to validate biological result data and observation-target references.

### UCI Gas Sensor Array Drift at Different Concentrations

Electronic-olfaction dataset used to validate composite observation targets and nested sensor-array results. UCI Machine Learning Repository, dataset 270, DOI 10.24432/C5MK6M.
